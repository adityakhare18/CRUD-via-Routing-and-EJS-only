<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white p-6">
      <div class="form p-10 bg-zinc-800 rounded-xl shadow-md max-w-xl mx-auto">
        <form action="/create" method="post">
          <input
            class="block w-full px-5 py-3 bg-zinc-700 rounded-lg placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-violet-500"
            type="text"
            name="title"
            placeholder="Title goes here"
          />
          <textarea
            class="block mt-4 w-full px-5 py-3 resize-none bg-zinc-700 rounded-lg h-32 placeholder:text-zinc-400 focus:outline-none focus:ring-2 focus:ring-violet-500"
            placeholder="Write your task details here"
            name="details"
          ></textarea>
          <input
            class="mt-4 w-full px-5 py-3 bg-violet-600 hover:bg-violet-700 text-white font-semibold rounded-lg cursor-pointer transition duration-300"
            type="submit"
            value="Create tasks"
          />
        </form>
      </div>

      <div class="tasks flex flex-wrap gap-6 mt-10 justify-center">
        <% if (files.length > 0) { %> <% files.forEach(file => { %>
        <div
          class="task w-72 p-6 rounded-xl bg-zinc-800 shadow-lg hover:shadow-xl transition duration-300 space-y-4"
        >
          <h1 class="text-xl font-bold text-white truncate"><%= file %></h1>

          <div class="flex flex-col gap-2">
            <a
              href="/file/<%= file %>"
              class="text-violet-400 hover:text-violet-300 text-sm font-medium inline-flex items-center"
            >
              📄 Read more
            </a>

            <a
              href="/edit/<%= file %>"
              class="text-blue-400 hover:text-blue-300 text-sm font-medium inline-flex items-center"
            >
              ✏️ Edit
            </a>

            <a
              href="/delete/<%= file %>"
              onclick="return confirm('Are you sure you want to delete this file?')"
              class="text-red-400 hover:text-red-300 text-sm font-medium inline-flex items-center"
            >
              🗑️ Delete
            </a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <div class="w-full text-center text-zinc-400 text-lg italic">
          No task yet
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
